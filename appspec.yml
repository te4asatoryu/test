version: 0.0
os: linux
files:
  - source: ./build/libs/demo-0.0.1-SNAPSHOT.jar
    destination: /tmp
hooks:
  ApplicationStop:
    - location: stopapp.sh
      timeout: 300
      runas: root
  ApplicationStart:
    - location: startapp.sh
      timeout: 300
      runas: root
